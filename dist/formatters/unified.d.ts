import { DiffResult } from '../types';
/**
 * Render a unified diff with ANSI colors.
 * Shows removed lines in red, added lines in green, with word-level highlighting.
 */
export declare function renderUnifiedDiff(result: DiffResult, useColor?: boolean): string;
//# sourceMappingURL=unified.d.ts.map